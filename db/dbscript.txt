-- DROP SCHEMA dbo;

CREATE SCHEMA dbo;
-- USERS.dbo.USER_TYPES definition

-- Drop table

-- DROP TABLE USERS.dbo.USER_TYPES;

CREATE TABLE USERS.dbo.USER_TYPES (
	ID int NOT NULL,
	[TYPE] varchar(1) COLLATE Modern_Spanish_CI_AS NULL,
	CONSTRAINT USER_TYPES_PK PRIMARY KEY (ID)
);


-- USERS.dbo.USERS definition

-- Drop table

-- DROP TABLE USERS.dbo.USERS;

CREATE TABLE USERS.dbo.USERS (
	ID int NOT NULL,
	PASSWORD varchar(200) COLLATE Modern_Spanish_CI_AS NOT NULL,
	FIRST_NAME varchar(50) COLLATE Modern_Spanish_CI_AS NULL,
	LAST_NAME varchar(50) COLLATE Modern_Spanish_CI_AS NULL,
	EMAIL varchar(50) COLLATE Modern_Spanish_CI_AS NULL,
	USER_TYPE_ID int NULL,
	ENTRY_DATE date NULL,
	ACTIVE bit NULL,
	USER_NAME varchar(50) COLLATE Modern_Spanish_CI_AS NULL,
	CONSTRAINT PK__USERS__3214EC27797A6133 PRIMARY KEY (ID),
	CONSTRAINT FK_USER_TYPE_ID FOREIGN KEY (USER_TYPE_ID) REFERENCES USERS.dbo.USER_TYPES(ID)
);
